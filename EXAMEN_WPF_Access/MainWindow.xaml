<Window x:Class="EXAMEN_WPF_Access.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sistema de Proveedores - Examen Parcial" Height="600" Width="800"
        FontFamily="Segoe UI" FontSize="14" 
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#3A86FF"/>
        <SolidColorBrush x:Key="SecondaryBlue" Color="#87CEEB"/>
        <SolidColorBrush x:Key="TextLight" Color="White"/>
        <SolidColorBrush x:Key="TextDark" Color="#333333"/>
        <SolidColorBrush x:Key="HighlightColor" Color="#66B2FF"/>

        <LinearGradientBrush x:Key="WindowBackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F0F4F8" Offset="0"/>
            <GradientStop Color="#FFFFFF" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HighlightColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2979FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Background>
        <StaticResource ResourceKey="WindowBackgroundGradient"/>
    </Window.Background>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Gestión de Proveedores" Grid.Row="0" Grid.ColumnSpan="2" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextDark}" HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,0">
            <Label Content="Nombre:" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
            <TextBox x:Name="txtNombre" Style="{StaticResource ModernTextBoxStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,0,0,0">
            <Label Content="Código Postal:" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
            <TextBox x:Name="txtCPost" Style="{StaticResource ModernTextBoxStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,15,0">
            <Label Content="Productos:" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
            <TextBox x:Name="txtProduc" Style="{StaticResource ModernTextBoxStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="1" Margin="15,0,0,0">
            <Label Content="Ubicación:" Foreground="{StaticResource TextDark}" Margin="0,0,0,5"/>
            <TextBox x:Name="txtUbica" Style="{StaticResource ModernTextBoxStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,25">
            <Button x:Name="btnInsertar" Content="Insertar" Style="{StaticResource ModernButtonStyle}" Click="btnInsertar_Click"/>
            <Button x:Name="btnMostrar" Content="Mostrar" Style="{StaticResource ModernButtonStyle}" Click="btnMostrar_Click"/>
            <Button x:Name="btnActualizar" Content="Actualizar" Style="{StaticResource ModernButtonStyle}" Click="btnActualizar_Click"/>
            <Button x:Name="btnEliminar" Content="Eliminar" Style="{StaticResource ModernButtonStyle}" Click="btnEliminar_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgvProveedores" Grid.Row="4" Grid.ColumnSpan="2" IsReadOnly="True" AutoGenerateColumns="True" SelectionMode="Single"
                  BorderThickness="1" BorderBrush="#E0E0E0" AlternatingRowBackground="#F9F9F9"
                  HorizontalGridLinesBrush="#EEEEEE" VerticalGridLinesBrush="#EEEEEE"
                  Margin="0,0,0,20" Foreground="{StaticResource TextDark}" SelectionChanged="dgvProveedores_SelectionChanged" >
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="White"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource SecondaryBlue}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
        </DataGrid>

        <Button x:Name="btnSalir" Content="Salir" Click="btnSalir_Click" Grid.Row="5" Grid.ColumnSpan="2" HorizontalAlignment="Center" Padding="40,8"
                Style="{StaticResource ModernButtonStyle}" BorderThickness="2" BorderBrush="{StaticResource PrimaryBlue}" Background="White" Foreground="{StaticResource PrimaryBlue}">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="15" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SecondaryBlue}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBlue}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

    </Grid>
</Window>